const url: string = 'https://vica.website'

const signupURL: string = `${url}/api/register`
const loginURL: string = `${url}/api/login`
const logoutURL: string = `${url}/api/logout`


export { signupURL, loginURL, logoutURL }